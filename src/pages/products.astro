---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE } from '../consts';
---

<!DOCTYPE html>
<html lang="en">
<head>
	<BaseHead 
		title={`Certified Security Hardware | ${SITE_TITLE}`}
		description="FIPS 140-3 Level 3 certified hardware-encrypted storage devices and security products for enterprise and government use."
	/>
</head>
<body>
	<Header />
	<main>
		<section class="hero">
			<div class="container">
				<h1>Certified Security Hardware</h1>
				<p class="tagline">Independent recommendations for high-assurance data protection</p>
			</div>
		</section>

		<section class="section">
			<div class="container">
				<p>We analyze and evaluate certified security products and recommend solutions that meet stringent protection requirements. We do not sell devices — our recommendations are based purely on independent technical evaluation.</p>
			</div>
		</section>

		<section class="section">
			<div class="container">
				<h2>Recommended Hardware</h2>
				
				<div class="card product-card">
					<div class="product-header">
						<h3>iStorage datAshur PRO+C</h3>
						<span class="badge">FIPS 140-3 Level 3 Certified</span>
					</div>
					<p class="product-intro">The datAshur PRO+C is one of the first portable storage devices to achieve FIPS 140-3 Level 3 validation — the highest assurance level currently available for commercial removable media.</p>
					
					<div class="specs">
						<div class="spec-item">
							<strong>Independent Certification</strong>
							<p>Validated to FIPS 140-3 Level 3, including hardened tamper-resistant construction, role-based authentication, and secure key storage.</p>
						</div>
						
						<div class="spec-item">
							<strong>Encryption Architecture</strong>
							<p>Full-disk AES-XTS 256-bit hardware encryption performed entirely on the device, ensuring encryption keys never touch a host system.</p>
						</div>
						
						<div class="spec-item">
							<strong>Tamper-Resistant Electronics</strong>
							<p>Internal components are encapsulated in a protective resin barrier designed to reveal and resist probing, drilling, voltage manipulation, or physical intrusion.</p>
						</div>
						
						<div class="spec-item">
							<strong>On-Device PIN Entry</strong>
							<p>Access is controlled through an integrated keypad, preventing keylogging and ensuring authentication occurs before the device connects to any computer.</p>
						</div>
						
						<div class="spec-item">
							<strong>Brute-Force Protection</strong>
							<p>After a defined number of incorrect PIN attempts, the device cryptographically erases its keys and permanently prevents further access.</p>
						</div>
						
						<div class="spec-item">
							<strong>System Compatibility</strong>
							<p>OS-agnostic operation. Works with Windows, macOS, Linux, thin clients, virtualised systems, and embedded devices without drivers or software.</p>
						</div>
					</div>
				</div>
			</div>
		</section>

		<section class="section">
			<div class="container">
				<div class="notice">
					<h3>Important Notice</h3>
					<p>We are not an authorised reseller or distributor of the datAshur PRO+C or any other encrypted storage device. Our recommendation is based purely on independent technical evaluation and suitability for high-assurance data protection requirements.</p>
				</div>
			</div>
		</section>

		<section class="section">
			<div class="container text-center">
				<h2>Determine Your Security Requirements</h2>
				<p>Not every organisation requires FIPS 140-3 validated hardware. We can help you assess whether your operational risk profile, compliance regime, and threat exposure justify a certified device of this class.</p>
				<div class="cta-buttons">
					<a href="/services" class="btn primary">View Our Services</a>
					<a href="mailto:hello@hermetrix.nz" class="btn secondary">Contact Us</a>
				</div>
			</div>
		</section>
	</main>
	<Footer />

	<style>
		.hero {
			padding: var(--space-3xl) 0 var(--space-2xl);
		}

		.tagline {
			font-size: 1.25rem;
			color: var(--text-secondary);
			margin-top: var(--space-md);
		}

		.product-card {
			margin-top: var(--space-2xl);
		}

		.product-header {
			display: flex;
			justify-content: space-between;
			align-items: center;
			margin-bottom: var(--space-lg);
			flex-wrap: wrap;
			gap: var(--space-md);
		}

		.product-header h3 {
			margin: 0;
			color: var(--text-primary);
		}

		.badge {
			display: inline-block;
			padding: var(--space-sm) var(--space-md);
			background: rgba(88, 166, 255, 0.1);
			border: 1px solid var(--accent);
			border-radius: var(--border-radius);
			font-size: 0.875rem;
			font-weight: 600;
			color: var(--accent);
		}

		.product-intro {
			font-size: 1.125rem;
			margin-bottom: var(--space-xl);
		}

		.specs {
			display: grid;
			grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
			gap: var(--space-lg);
		}

		.spec-item {
			padding: var(--space-lg);
			background: var(--background);
			border: 1px solid var(--border-color);
			border-radius: var(--border-radius);
		}

		.spec-item strong {
			display: block;
			color: var(--accent);
			margin-bottom: var(--space-sm);
			font-size: 1rem;
		}

		.spec-item p {
			margin: 0;
			font-size: 0.9375rem;
			line-height: 1.6;
		}

		.notice {
			padding: var(--space-xl);
			background: var(--background-alt);
			border-left: 3px solid var(--accent);
			border-radius: var(--border-radius);
		}

		.notice h3 {
			color: var(--accent);
			margin-top: 0;
		}

		.notice p {
			margin-bottom: 0;
		}

		.cta-buttons {
			display: flex;
			gap: var(--space-md);
			justify-content: center;
			margin-top: var(--space-xl);
		}

		@media (max-width: 768px) {
			.product-header {
				flex-direction: column;
				align-items: flex-start;
			}

			.specs {
				grid-template-columns: 1fr;
			}

			.cta-buttons {
				flex-direction: column;
				align-items: stretch;
			}

			.btn {
				width: 100%;
				text-align: center;
			}
		}
	</style>
</body>
</html>
