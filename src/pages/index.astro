---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION, CONTACT_EMAIL, SECURITY_EMAIL } from '../consts';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<header>
			<Header />
		</header>
		<main>
			<!-- Hero Section -->
			<section class="hero">
				<div class="container">
					<h1>Strategic Operational Security Consulting</h1>
					<p class="tagline">For Businesses That Take Protection Seriously</p>
					<p class="subtitle">
						We provide strategic OPSEC analysis and implementation for businesses who understand that security is more than just technology. We help identify and fix the human, process, and strategic vulnerabilities that traditional IT security misses.
					</p>
					<div class="cta-buttons">
						<a href="/services" class="btn primary">Schedule OPSEC Assessment</a>
						<a href="/about" class="btn secondary">Our Approach</a>
					</div>
				</div>
			</section>

			<!-- Services Overview -->
			<section class="services-overview">
				<div class="container">
					<h2>Our OPSEC Consulting Framework</h2>
					<div class="services-grid">
						<div class="service-card">
							<h3>Operational Security Analysis</h3>
							<p>Understanding what you're truly protecting beyond data—identifying who would target you and why, finding overlooked vulnerabilities in people, processes, and technology, and mapping your complete digital footprint.</p>
							<a href="/services#analysis" class="learn-more">Learn more →</a>
						</div>
						<div class="service-card">
							<h3>Layered Security Solutions</h3>
							<p>Immediate risk mitigation (0-30 days), strategic security roadmap (90 days - 12 months), ongoing security operations, and seamless integration of our products with partner solutions.</p>
							<a href="/services#solutions" class="learn-more">Learn more →</a>
						</div>
						<div class="service-card">
							<h3>Implementation & Enablement</h3>
							<p>Hands-on deployment support, security protocol development, employee training and awareness programs, and continuous improvement initiatives to ensure sustainable protection.</p>
							<a href="/services#implementation" class="learn-more">Learn more →</a>
						</div>
					</div>
				</div>
			</section>

			<!-- Why Choose Us -->
			<section class="why-us">
				<div class="container">
					<h2>Why Hermetrix Technologies</h2>
					<div class="features-grid">
						<div class="feature">
							<h3>Security-First Approach</h3>
							<p>Every solution is evaluated against certified security standards and framework requirements.</p>
						</div>
						<div class="feature">
							<h3>APAC Regional Focus</h3>
							<p>Deep understanding of New Zealand and Asia-Pacific security requirements and regulations.</p>
						</div>
						<div class="feature">
							<h3>Enterprise Expertise</h3>
							<p>Solutions designed for business and government environments with complex security needs.</p>
						</div>
						<div class="feature">
							<h3>Practical Guidance</h3>
							<p>Helping organizations understand and work toward security compliance through appropriate solutions.</p>
						</div>
					</div>
				</div>
			</section>

			<!-- Contact CTA -->
			<section class="contact-cta">
				<div class="container">
					<h2>Ready for Strategic OPSEC Assessment?</h2>
					<p>Security-conscious leaders trust us to identify and address the vulnerabilities that matter most to their business. Let's discuss how we can protect your operations.</p>
					<div class="contact-info">
						<div class="contact-method">
							<strong>Schedule Assessment</strong>
							<a href={`mailto:${CONTACT_EMAIL}`}>{CONTACT_EMAIL}</a>
						</div>
						<div class="contact-method">
							<strong>Security Consultations</strong>
							<a href={`mailto:${SECURITY_EMAIL}`}>{SECURITY_EMAIL}</a>
						</div>
					</div>
					<div class="cta-buttons">
						<a href="/services" class="btn primary">View Our Services</a>
						<a href="/about" class="btn secondary">Learn Our Approach</a>
					</div>
				</div>
			</section>
		</main>
		<footer>
			<Footer />
		</footer>

		<style>
			/* Full-screen layout */
			body {
				margin: 0;
				min-height: 100vh;
				display: flex;
				flex-direction: column;
			}

			main {
				flex: 1;
				display: flex;
				flex-direction: column;
			}

			/* Hero Section - Full width, high-tech */
			.hero {
				min-height: 100vh;
				display: flex;
				align-items: center;
				justify-content: center;
				text-align: center;
				background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 50%, #2a2a2a 100%);
				color: white;
				position: relative;
				overflow: hidden;
				width: 100vw;
				margin-left: calc(50% - 50vw);
			}

			.hero::before {
				content: "";
				position: absolute;
				top: 0; left: 0; right: 0; bottom: 0;
				background: radial-gradient(circle at 20% 80%, rgba(0, 212, 255, 0.1) 0%, transparent 50%),
							radial-gradient(circle at 80% 20%, rgba(0, 212, 255, 0.1) 0%, transparent 50%),
							radial-gradient(circle at 40% 40%, rgba(0, 212, 255, 0.05) 0%, transparent 50%);
				z-index: 0;
			}

			.hero > .container {
				position: relative;
				z-index: 1;
				padding: 0 2rem;
				max-width: 1200px;
			}

			.hero h1 {
				font-size: 5rem;
				margin-bottom: 1.5rem;
				font-weight: 900;
				color: #ffffff;
				text-shadow: 0 0 30px rgba(0, 212, 255, 0.5), 0 0 60px rgba(0, 212, 255, 0.3);
				letter-spacing: 0.02em;
				line-height: 1.1;
			}

			.tagline {
				font-size: 2.5rem;
				font-weight: 400;
				margin-bottom: 1.5rem;
				color: var(--accent);
				text-shadow: 0 0 20px rgba(0, 212, 255, 0.5);
			}

			.subtitle {
				font-size: 1.5rem;
				max-width: 800px;
				margin: 0 auto 3rem;
				color: var(--text-secondary);
				line-height: 1.7;
			}

			.cta-buttons {
				margin-top: 2rem;
				display: flex;
				gap: 1rem;
				justify-content: center;
				flex-wrap: wrap;
			}

			.btn {
				display: inline-block;
				padding: 1rem 2.5rem;
				text-decoration: none;
				border-radius: 0.5rem;
				font-weight: 600;
				transition: all 0.3s ease;
				font-size: 1.1rem;
				border: 2px solid transparent;
			}

			.btn.primary {
				background: var(--accent);
				color: #0a0a0a;
				box-shadow: 0 0 20px rgba(0, 212, 255, 0.3);
			}

			.btn.primary:hover {
				background: var(--accent-dark);
				box-shadow: 0 0 40px rgba(0, 212, 255, 0.6);
				transform: translateY(-2px);
			}

			.btn.secondary {
				background: transparent;
				color: var(--accent);
				border-color: var(--accent);
				box-shadow: 0 0 10px rgba(0, 212, 255, 0.2);
			}

			.btn.secondary:hover {
				background: var(--accent);
				color: #0a0a0a;
				box-shadow: 0 0 30px rgba(0, 212, 255, 0.5);
				transform: translateY(-2px);
			}

			/* Services Overview - Compact grid */
			.services-overview {
				padding: 5rem 2rem;
				background: var(--bg-secondary);
				min-height: 80vh;
				display: flex;
				align-items: center;
			}

			.services-overview h2 {
				text-align: center;
				margin-bottom: 4rem;
				font-size: 3.5rem;
				color: var(--text-primary);
				font-weight: 700;
				letter-spacing: 0.04em;
				text-shadow: 0 0 20px rgba(0, 212, 255, 0.3);
			}

			.services-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
				gap: 2rem;
				width: 100%;
				max-width: 1400px;
				margin: 0 auto;
			}

			.service-card {
				padding: 3rem 2rem;
				background: rgba(26, 26, 26, 0.8);
				backdrop-filter: blur(10px);
				border-radius: 1rem;
				box-shadow: var(--box-shadow);
				border: 1px solid rgba(0, 212, 255, 0.3);
				transition: all 0.3s ease;
				position: relative;
				overflow: hidden;
			}

			.service-card::before {
				content: '';
				position: absolute;
				top: 0;
				left: 0;
				right: 0;
				height: 4px;
				background: linear-gradient(90deg, var(--accent), var(--accent-dark));
			}

			.service-card:hover {
				transform: translateY(-10px);
				box-shadow: 0 20px 60px rgba(0, 212, 255, 0.4);
				border-color: var(--accent);
			}

			.service-card h3 {
				color: var(--text-primary);
				margin-bottom: 1.5rem;
				font-size: 1.6rem;
				font-weight: 600;
				letter-spacing: 0.02em;
			}

			.service-card p {
				color: var(--text-secondary);
				line-height: 1.7;
			}

			.learn-more {
				color: var(--accent);
				font-weight: 600;
				text-decoration: none;
				display: inline-block;
				margin-top: 1.5rem;
				transition: all 0.3s ease;
			}

			.learn-more:hover {
				color: var(--accent-dark);
				text-shadow: 0 0 10px rgba(0, 212, 255, 0.5);
			}

			/* Why Us Section - Full width grid */
			.why-us {
				padding: 5rem 2rem;
				background: var(--bg-primary);
				min-height: 80vh;
				display: flex;
				flex-direction: column;
				justify-content: center;
			}

			.why-us h2 {
				text-align: center;
				margin-bottom: 4rem;
				font-size: 3.5rem;
				color: var(--text-primary);
				font-weight: 700;
				letter-spacing: 0.04em;
				text-shadow: 0 0 20px rgba(0, 212, 255, 0.3);
			}

			.features-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
				gap: 2rem;
				width: 100%;
				max-width: 1600px;
				margin: 0 auto;
			}

			.feature {
				text-align: center;
				padding: 3rem 2rem;
				border-radius: 1rem;
				background: rgba(26, 26, 26, 0.8);
				backdrop-filter: blur(10px);
				box-shadow: var(--box-shadow);
				border: 1px solid rgba(0, 212, 255, 0.2);
				transition: all 0.3s ease;
				position: relative;
			}

			.feature:hover {
				transform: translateY(-5px);
				box-shadow: 0 15px 45px rgba(0, 212, 255, 0.3);
				border-color: var(--accent);
			}

			.feature h3 {
				color: var(--text-primary);
				margin-bottom: 1.5rem;
				font-size: 1.5rem;
				font-weight: 600;
				letter-spacing: 0.02em;
			}

			.feature p {
				color: var(--text-secondary);
				line-height: 1.7;
			}

			/* Contact CTA - Full width */
			.contact-cta {
				padding: 5rem 2rem;
				background: var(--bg-secondary);
				color: white;
				text-align: center;
				min-height: 60vh;
				display: flex;
				flex-direction: column;
				justify-content: center;
			}

			.contact-cta h2 {
				margin-bottom: 1.5rem;
				color: var(--accent);
				font-size: 3rem;
				font-weight: 700;
				letter-spacing: 0.04em;
				text-shadow: 0 0 20px rgba(0, 212, 255, 0.5);
			}

			.contact-cta p {
				font-size: 1.3rem;
				color: var(--text-secondary);
				margin-bottom: 3rem;
				max-width: 700px;
				margin-left: auto;
				margin-right: auto;
				line-height: 1.7;
			}

			.contact-info {
				display: flex;
				justify-content: center;
				gap: 4rem;
				margin: 2rem 0;
				flex-wrap: wrap;
			}

			.contact-method {
				text-align: center;
			}

			.contact-method strong {
				display: block;
				margin-bottom: 0.5rem;
				color: var(--accent);
				font-size: 1.2rem;
			}

			.contact-cta a {
				color: var(--accent);
				text-decoration: none;
				font-weight: 500;
				transition: all 0.3s ease;
			}

			.contact-cta a:hover {
				color: var(--text-primary);
				text-shadow: 0 0 10px rgba(0, 212, 255, 0.5);
			}

			.container {
				width: 100%;
				max-width: none;
			}

			/* Responsive Design */
			@media (max-width: 1024px) {
				.hero h1 {
					font-size: 4rem;
				}
				.tagline {
					font-size: 2rem;
				}
				.subtitle {
					font-size: 1.3rem;
				}
				.services-overview h2,
				.why-us h2 {
					font-size: 3rem;
				}
				.contact-cta h2 {
					font-size: 2.5rem;
				}
			}

			@media (max-width: 768px) {
				.hero {
					padding: 2rem 1rem;
					min-height: 90vh;
				}
				.hero h1 {
					font-size: 3rem;
				}
				.tagline {
					font-size: 1.8rem;
				}
				.subtitle {
					font-size: 1.2rem;
				}
				.cta-buttons {
					flex-direction: column;
					align-items: center;
				}
				.btn {
					width: 250px;
				}
				.contact-info {
					flex-direction: column;
					gap: 2rem;
				}
				.services-grid,
				.features-grid {
					grid-template-columns: 1fr;
					gap: 1.5rem;
				}
				.services-overview,
				.why-us,
				.contact-cta {
					padding: 3rem 1rem;
					min-height: auto;
				}
				.services-overview h2,
				.why-us h2 {
					font-size: 2.5rem;
					margin-bottom: 2rem;
				}
				.contact-cta h2 {
					font-size: 2rem;
				}
			}

			@media (max-width: 480px) {
				.hero h1 {
					font-size: 2.5rem;
				}
				.tagline {
					font-size: 1.5rem;
				}
				.subtitle {
					font-size: 1.1rem;
				}
				.services-overview h2,
				.why-us h2 {
					font-size: 2rem;
				}
				.contact-cta h2 {
					font-size: 1.8rem;
				}
				.btn {
					padding: 0.8rem 2rem;
					font-size: 1rem;
				}
			}
		</style>
	</body>
</html>
