---
---
<style>
:root{--primary:#1e40af;--secondary:#ffffff;--accent:#10b981;--text:#374151}
/* Business-mode wrapper - scope styles under .business-mode to avoid global changes */
.business-mode{background:var(--secondary);color:var(--text);font-family:Inter,ui-sans-serif,system-ui,-apple-system,'Segoe UI',Roboto,'Helvetica Neue',Arial}
.business-mode .container{max-width:1200px;margin:0 auto;padding:2rem}
.business-mode h1, .business-mode h2, .business-mode h3{color:var(--text)}
.business-mode .lead{color:var(--primary)}
.business-mode .btn.primary{background:var(--primary);color:var(--secondary);border-radius:10px;padding:.75rem 1.25rem;text-decoration:none;display:inline-block}
.business-mode .btn.primary:hover{filter:brightness(.97);transform:translateY(-2px)}
.business-mode .btn.ghost{background:transparent;border:2px solid var(--primary);color:var(--primary);border-radius:10px;padding:.5rem .85rem}
.business-mode .card, .business-mode .core-offer, .business-mode .service-card{border-radius:12px;box-shadow:0 8px 24px rgba(16,24,40,0.06);}
.business-mode .partner-badge{display:inline-block}
/* Utility spacing tweaks for business pages */
.business-mode .actions{display:flex;gap:1rem;justify-content:center}
@media(max-width:640px){.business-mode .container{padding:1rem}}
</style>